import React from "react";

import { Box } from "@mui/material";

const Feed = () => {
  return (
    <Box flex={2} p={2} position="sticky" top={0}>
      Feed
    </Box>
  );
};

export default Feed;
